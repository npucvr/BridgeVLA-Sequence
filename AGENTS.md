# AGENTS.md — BridgeVLA

## 定位

本文件是给代码 Agent 使用的**协作协议**，只记录稳定、可复用的约束，不承担项目手册或运行环境清单的职责。

- 架构、入口、参数和实验步骤以源码、配置、`README.md`、`docs/` 及现有脚本为准。
- 可复现命令写入 `README.md`、`docs/` 或 `scripts/`；不要把一次性命令、审计结果或当前资源占用写进本文件。
- 当前可用于运行任务的 GPU 节点是 `server16`、`server17`、`server19` 和 `server20`；节点的 GPU 型号、空闲状态和显存占用必须在实际运行前重新核验。
- 四个节点通过同一个 NFS 共享仓库，统一目录为 `/remote_userdata/sunguodong/repos/BridgeVLA`；不要改用只在某一台机器存在的本地路径。
- conda 安装路径、显示编号等机器私有差异使用运行时脚本支持的用户配置解决，不提交包含私有路径或凭据的配置。

## 修改协议

- 修改前先读取相关源码、配置和文档；保留工作区中与当前任务无关的改动，不覆盖用户产物。
- 优先做最小、局部、可回滚的修改；改变公共接口或核心算法前，检查调用方、配置和 checkpoint 兼容性。
- 优先复用现有入口、配置和脚本，不重复实现已有机制；修改内置或第三方代码前说明必要性。
- `agent_playground/` 是 AI 的自由工作区；AI 创建的临时脚本、审计脚本、临时配置、日志、中间文件和可视化预览统一优先放在这里，不要为了这些内容使用 `/tmp`，也不要散落在仓库根目录或核心源码目录。
- `agent_playground/` 中的内容默认不构成正式接口；该目录已由根目录 `.gitignore` 排除，不要手动将其加入 Git 或 commit；任务结束后删除无用产物，需要长期复用的脚本再提升到 `scripts/` 并补充说明。
- 不把模型权重、原始数据、正式实验结果或其他大文件复制到 `agent_playground/`，也不将它们加入源码改动。

## 版本控制协议

- 不要擅自执行 `git commit` 或 `git push`；执行前必须与用户核实提交范围、commit message、目标分支和远端。
- commit 前只暂存本次确认的文件，不使用 `git add .` 或其他方式混入无关改动；发现工作区有用户修改时先保留并说明。
- 除非用户明确要求，不执行强制 push、丢弃工作区修改或改写远端共享历史。

## 运行协议

- 训练、评估或其他长时间任务使用仓库提供的运行时入口 `scripts/bridgevla_runtime.sh`，不要依赖被忽略的个人环境脚本。
- 真正启动资源密集型任务前，在**同一个执行 shell** 中确认当前 `hostname`、仓库 `pwd`、GPU 状态及已有进程；不得凭历史记录假定节点或显存可用。
- 若通过 SSH 使用其他节点，远程命令也必须重新确认 hostname、仓库路径和 GPU；当前节点无需 SSH 回自己。
- RLBench、Colosseum、GemBench 的仿真依赖相互隔离，使用各自的环境和配置；并发 CoppeliaSim 实例必须隔离 GPU 与 display。
- 使用 gated 模型或外部数据前先确认认证、路径和版本；不要把本机可用性当作所有环境都可用。

## 文档与验证协议

- 新增或修改文档默认使用中文；代码标识符、路径、命令、任务名、模型名、数据集名、版本号、链接和公式保持原文及技术含义。
- 行为、接口或运行约定改变时同步更新对应的正式文档；纯临时过程不升级为正式接口。
- 根据改动运行最小且相关的验证（测试、导入检查、shell 检查或 smoke test）；如项目没有现成检查，明确记录未验证项，不夸大结果。
- 完成工作时报告：改动摘要、文件路径、已执行的验证、未执行项及仍存在的环境假设或阻塞。
